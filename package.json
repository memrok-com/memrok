{
  "name": "memrok",
  "version": "0.3.1",
  "private": true,
  "description": "Cognitive prosthetic memory layer for AI agents",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/memrok-com/memrok.git"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "vitest": "^4.1.2"
  }
}
