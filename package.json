{
  "name": "memrok",
  "version": "0.5.1",
  "private": true,
  "description": "Memory with judgment. Graph-based curation layer for OpenClaw agents, complementing built-in recall and dreaming.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "hygiene:broad-nodes": "npx tsx scripts/hygiene-broad-nodes.ts"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/memrok-com/memrok.git"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "vitest": "^4.1.2"
  }
}
