name: Validate ClawHub Publish (manual only)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "ClawHub auto-publish is intentionally paused."
          echo "Reason: monorepo TypeScript/workspace build is not yet reproducible in GitHub Actions."
          echo "Next step: fix CI structure first, then restore publish-on-tag."