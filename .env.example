# Auth Secrets (generate these with: openssl rand -hex 32)
JWT_SECRET=your-random-jwt-secret-here
SESSION_SECRET=your-random-session-secret-here
STORAGE_ENCRYPTION_KEY=your-random-storage-key-here

# Domain Configuration
MEMROK_APP_DOMAIN=app.yourdomain.com
MEMROK_AUTH_DOMAIN=auth.yourdomain.com

# Docker Configuration  
DOCKER_NETWORK=memrok

# Let's Encrypt Configuration
EMAIL=your-email@example.com

# DNS Provider for Let's Encrypt DNS Challenge
# See https://doc.traefik.io/traefik/https/acme/#providers for full list
DNS_PROVIDER=cloudflare

# DNS Provider API Credentials
# Examples by provider:
# Cloudflare:
CLOUDFLARE_DNS_API_TOKEN=your-dns-provider-api-token

# Route53:
# DNS_PROVIDER=route53
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1

# DigitalOcean:
# DNS_PROVIDER=digitalocean
# DO_AUTH_TOKEN=your-do-token